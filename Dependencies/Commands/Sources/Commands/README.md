# Commands

## Purpose
`Commands` is the core command framework used by Stack.

It defines command types, command availability, confirmation behavior, and the command-centre interface for executing actions.

## Responsibilities
- Define command protocols and core command metadata.
- Define command dispatch and center abstractions.
- Model command availability and confirmation requirements.

## Depends On
- `Logger`
